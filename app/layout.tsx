import type React from "react"
import type { Metadata } from "next"
import { Inter, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _inter = Inter({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Ewolw | 360° Digital & AI Solutions",
  description:
    "Ewolw provides cutting-edge digital transformation, AI solutions, and brand growth strategies to help businesses evolve and scale.",
  keywords: "Digital Transformation, AI Solutions, Brand Growth, Innovation, Automation, Data-Driven, Future-Ready",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
